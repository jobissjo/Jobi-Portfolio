import { AtroposDirective } from './atropos.directive';

describe('AtroposDirective', () => {
  it('should create an instance', () => {
    const directive = new AtroposDirective();
    expect(directive).toBeTruthy();
  });
});
